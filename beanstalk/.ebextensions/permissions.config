container_commands:
  01_change_storage_permissions:
    # Get the php fpm container ID and change permissions on the mounted directories
    command: sudo chown -R 1000:1000 /var/app/assets /var/app/database